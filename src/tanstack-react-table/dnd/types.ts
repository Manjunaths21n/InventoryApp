import { ReactNode } from 'react';

export interface IDraggableProps {
    id: string | number;
    children: ReactNode
}
export interface IDroppbleProps {
    id: string | number;
    children: ReactNode
}